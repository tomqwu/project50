# Next.js generates this file automatically — it uses triple-slash references
# by design and must not be edited (see nextjs.org/docs/app/api-reference/config/typescript).
next-env.d.ts

# Generated coverage reports (istanbul JS) — not source, must not be linted.
coverage/

# k6 load-test scripts run in the k6 runtime (standalone binary), not Node.
# They import from `k6/*` which ESLint cannot resolve, so exclude them.
load-test/
