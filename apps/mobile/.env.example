# Expo public env — inlined into the bundle at build time. NEVER put the App Secret here.
# After changing these, fully restart the bundler: `expo start -c`.
EXPO_PUBLIC_FACEBOOK_APP_ID=
EXPO_PUBLIC_API_BASE_URL=http://localhost:3000
# EAS projectId for Expo push tokens (getExpoPushTokenAsync). Required for real
# device push tokens in standalone/dev builds; unset is fine in Expo Go.
EXPO_PUBLIC_EAS_PROJECT_ID=
# Sentry DSN for crash/error reporting (@sentry/react-native). When set, the app
# initializes Sentry at startup; leave UNSET to fully disable reporting (no init)
# in dev/CI/Expo Go. Get the DSN from your Sentry project settings.
EXPO_PUBLIC_SENTRY_DSN=
# RevenueCat public API keys for in-app subscriptions (react-native-purchases —
# StoreKit on iOS, Play Billing on Android). The Upgrade screen + IAP layer are
# ACTIVE only when a key is set; leave ALL of these UNSET to fully disable IAP
# (graceful "unavailable" state, no native StoreKit/Play Billing handlers) in
# dev/CI/Expo Go. Set the platform-specific key per store (preferred); the shared
# key is the fallback when the platform key is unset. Get keys from the RevenueCat
# dashboard → Project settings → API keys (use the PUBLIC SDK keys).
EXPO_PUBLIC_REVENUECAT_IOS_KEY=
EXPO_PUBLIC_REVENUECAT_ANDROID_KEY=
EXPO_PUBLIC_REVENUECAT_KEY=
