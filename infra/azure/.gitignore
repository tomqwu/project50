# Terraform working files — never commit state or local plans (may contain secrets)
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
# Keep .terraform.lock.hcl committed (provider pinning).
tfplan
